import React from 'react'
import ReactDOM from 'react-dom/client'
import FindCommandBuilder from './FindCommandBuilder'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <FindCommandBuilder />
  </React.StrictMode>
)

